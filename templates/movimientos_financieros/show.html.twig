{% extends 'base.html.twig' %}

{% block title %}MovimientosFinancieros{% endblock %}

{% block body %}
    <h1>MovimientosFinancieros</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movimientos_financiero.id }}</td>
            </tr>
            <tr>
                <th>Importe</th>
                <td>{{ movimientos_financiero.Importe }}</td>
            </tr>
            <tr>
                <th>Concepto</th>
                <td>{{ movimientos_financiero.Concepto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_movimientos_financieros_index') }}">back to list</a>

    <a href="{{ path('app_movimientos_financieros_edit', {'id': movimientos_financiero.id}) }}">edit</a>

    {{ include('movimientos_financieros/_delete_form.html.twig') }}
{% endblock %}
