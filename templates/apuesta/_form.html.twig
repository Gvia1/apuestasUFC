<div class="container">
    <div class="row">
        <div class="col-md-5 mx-auto">
        <div id="first">
            <div class="myform form text-light">
                <div class="logo mb-3">
                    <div class="col-md-12 text-center">
                        <h1 class="text-light">Apuesta</h1>
                    </div>
                </div>
                {{ form_start(form) }}
                <div class="form-group">
                    <label>Ganador</label><br>
                    {{ form_widget(form.ganador)}}
                </div>
                <div class="form-group">
                    <label>Round</label><br>
                    {{ form_widget(form.round)}}
                </div>
                <div class="form-group">
                    <label>Metodo</label><br>
                    {{ form_widget(form.metodo)}}
                </div>
                <div class="form-group">
                    <label>Metodo Especifico</label><br>
                    {{ form_widget(form.metodoEspecifico)}}
                </div>
                <div class="form-group">
                    <label>Cantidad</label><br>
                    {{ form_widget(form.cantidad)}}
                </div>
                {{ form_end(form) }}
                <div class="col-md-12 text-center ">
                    <button type="submit" class=" btn btn-block mybtn btn-danger tx-tfm">Apostar!!!</button>
                </div>
            </div>
        </div>
    </div>
</div>